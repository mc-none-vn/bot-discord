#v2 #00ccff
$removeAllComponents
$var[emoji;$serverEmojis[$guildID; ]]

$addTextDisplay[## **{emoji:emoji} | Emoji máy chủ**;main]
$addSeparator[true;;main]
$addTextDisplay[# $var[emoji];main]
$addActionRow[button;main]
$addButtonCV2[serverinfo$authorID-{data:id}_exit;Back;secondary;false;{emoji:back};button]
